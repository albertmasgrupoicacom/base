import { endpoints } from '../api/endpoints';
export const environment = {
    production: true,
    href: '.',
    api: {
        // base: 'http://localhost:8085/archetype',
        endpoints,
    },
};
